# Local PostgreSQL settings used by compose.local.yml.
# Copy this file to .env and replace the password. Never commit .env.
POSTGRES_DB=catchlogr_dev
POSTGRES_USER=catchlogr_user
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_PORT=5432
